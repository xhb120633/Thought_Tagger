# ThoughtTagger Studio

Researcher-facing UI for upload → configure → export.

## Status

MVP UI implemented for local researcher use:
- paste/upload study spec JSON
- paste/upload dataset text (`jsonl` or `csv`)
- live validation + compile preview (document/unit counts and unit samples)

## Run

```bash
npm install
npm run dev -w @thought-tagger/studio
```
